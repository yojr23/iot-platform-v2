# Gate 10 current fault matrix

No live-Docker run has been performed for this source change. This file is intentionally a generated target, not closure evidence.

Run `python scripts/gate10/fault_injection.py --scenario all` in an approved Docker-capable environment after supplying the required `GATE10_*` configuration and ensuring `git status --porcelain=v1 --untracked-files=no` is empty. The harness overwrites this file with the current SHA, per-scenario result, correlation IDs, and the path to its JSON evidence.
